name: flogin
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_facebook_auth: ^3.4.1
  fzregex: ^1.0.3
  google_fonts: ^2.1.0
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_rename_app:

flutter_rename_app:
  application_name: Facebook Login
  dart_package_name: flogin
  application_id: com.fz.flogin
  bundle_id: com.fz.flogin
  android_package_name: com.fz.flogin

flutter:
  uses-material-design: true

  assets:
    - assets/
